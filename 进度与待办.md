# 进度与待办（pokerPreFlop）

> 最后更新：2026-06-27。本文记录当前进度 + 待办，方便随时接着做。
> 配套：[CLAUDE.md](CLAUDE.md)（架构）、[PRODUCT.md](PRODUCT.md)（产品红线）。

---

## 一句话现状

**Nash 推弃图查询器（阶段1）已做完并本地 commit（`e843354`），但还没 push、没部署、没出新 app 包** —— 所以网页和现有 app 包都看不到 Nash 按钮。这是「看不到 Nash 按钮」的原因。

- 本地 HEAD：`e843354`（含 Nash）
- origin/main + EC2 线上：`8b50cb3`（**不含 Nash**）
- 当前版本号：**versionCode 7 / 1.6**（注：vc7 的 AAB 是 Nash 实现前打的，不含 Nash）

---

## 已完成

### Nash 推弃图查询器 · 阶段1（commit `e843354`，已验证）
- **solver 导出每手 EV**：`tools/pushfold.js` 的 `solveRing`/`solveHU` 收敛后导出 `jamEV`/`callerEV`（相对 fold 的增量，>0 即 +EV）
- **数据重新生成**：`js/data/pushfold.js`（181KB）+ `hu-pushfold.js`（61KB）带每手 chip-EV，频率不变、exploitability 近 0
- **Nash 查询页**：navrow 入口（♠ Nash）、5 模式（9max/6max/HU × open-jam/BB-call）× stack × position、13×13 EV 矩阵（绿红渐变 + 数字）+ +EV 覆盖率、双语、免费功能
- **验证**：测试 39/39；模拟器实测渲染正确；**EV 数学正确性已用 Nash indifference 验证通过**（混合手 EV≈0）
- **诚实点**：EV 是 no-ante 模型的 chip-EV（竞品数字更大是因为含 12.5% ante，阶段2 才加）

### 课金 / 双订阅（已实装 + 真机验证）
- 双订阅：年 `pro_yearly` $12.99（主推）+ 月 `pro_monthly` $4.99，**已移除一次买断**
- `USE_TEST_STORE = false`（真实 Play 计费），真机扣款→解锁全链路验证通过
- RevenueCat：凭证 Valid、两产品 Published + attach 到 `pro`、`default` offering = Current
- 本地通知（每日提醒 + 时间选择器）、字体离线内嵌、共享 cap.js helper —— 都已做 + 验证

### 文档
- CLAUDE.md / PRODUCT.md / ANDROID_SETUP.md / privacy.html / memory 已对齐双订阅
- 跨项目经验手册：`C:\Users\tangz\Documents\Capacitor-RevenueCat-上线手册.md`

---

## 待办（按优先级）

### A. 让 Nash 上线（立即可做，二选一或都做）
1. **部署网页**：`bash deploy.sh` → push 到 GitHub + EC2 pull，网页就有 Nash 了（网页恒免费，Nash 是很好的展示）
2. **出新 app 包**：bump `versionCode 8 / 1.7` → `build-www + cap copy + bundleRelease` → 传 Play

### B. Nash 阶段2（大工程，独立）
- solver 加 **ante 模型**（竞品有 12.5% ante，现在 no-ante）
- 补全维度：stack **5–20 每档** + 9-max position **全细分**（UTG1/MP1/MP2/MP3）
- 重算上千个 Nash 解 = **几小时离线计算**（固定种子跑一轮）
- 改 `tools/pushfold.js`（EV 公式加 ante 项）+ `gen-pushfold.js`（STACKS/SEAT_OF 扩展）

### C. Google Play 本番上架
- **12 个测试者 + 14 天 closed test**（Google 新规，公司账号也被要求；账号级一次性门槛）
  - 凑人：朋友家人 + 开发者互助群（r/AndroidAppTesters 等）。**别自己开 12 个账号**（反作弊风险）
- App content：Data safety 表单（按"不收集数据"如实填）、隐私政策 URL（已部署 `/privacy.html`）
- Content rating：选 **Reference to gambling**（非真钱、训练工具定位）
- Category：**Education** + tags **Brain games / Education**（不选 Casino）
- Store listing：文案已优化好（见对话/可重新生成），素材在 `store-assets/`
- App access：选 **Yes**（有付费内容），说明核心免费可玩、无需登录

---

## 关键技术备忘

- **命令**：测试 `node --test "test/*.test.js"`（39 项）；重算 Nash `node tools/gen-pushfold.js`（~4min）+ `gen-hu-pushfold.js`；出包见 CLAUDE.md
- **Node**：`C:\Program Files\nodejs\node.exe`（fresh shell 要全路径）
- **部署**：`bash deploy.sh "msg"`（gitignored，commit+test+push+EC2 pull）
- **版本号**：当前 7，**出新包必须 bump 到 8**（Play 拒绝重复 versionCode）
- **模拟器**：Pixel_7（`emulator -avd Pixel_7 -no-snapshot-load`）；这台账号持 active 月订阅 = Pro 全解锁；要看付费墙得临时把 `isPro` 改 false
- **提交规则**：只有用户说「提交/commit」才 commit；commit/push/部署后用 `git log`/HEAD 核实，绝不臆报
- **Nash 数据格式**：`PUSHFOLD.stacks[d].seatsEV[pos]` / `ring6[d].seatsEV[pos]` / `calloff[d].btnEV` / `HU_PUSHFOLD.stacks[d].jamEV`/`callEV` = 每手 EV（bb，相对弃牌）
