# 翻前诊断 + 20 天训练计划 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给翻前训练器加一条"首次问卷 → 诊断测试(简化18/详细45) → 报告 → 20天每日计划"的路径,免费做诊断+报告、Pro 解锁计划执行。

**Architecture:** 新增 `js/coach.js`(在 `app.js` 之后加载,共享全局作用域)放诊断聚合 / 计划编排 / 报告与每日卡片渲染。诊断走现有游戏循环但加 `G.diagMode` 隔离(不计 HP/生涯统计/常规错题堆)。纯逻辑函数(`coachScenes`/`coachAggregate`/`coachVerdict`/`coachBuildPlan`)无 DOM 依赖、可单元测试;UI 渲染沿用现有 `renderLeak`/`renderPlan` 的字符串拼接模式。

**Tech Stack:** 零构建单页、classic `<script>`、localStorage、`node --test` 回归(vm 加载、Proxy stub DOM)。

> ⚠️ **提交规则(本项目最高优先)**:只有用户明确说「提交/commit」才 `git commit`。下面每个 Task 末尾的 commit 步骤,平时**只把改动留在工作区等验收**,等用户发话再提交。`npm test` 该跑还是要跑。

> **诚实红线(PRODUCT.md §2/§6)**:报告/计划全程标「vs 参考范围」;报告里的百分比只是"正答率"(实测可算),**不是 EV/频率**;不 claim TAG/LAG;不造新数据,全复用现有 spot/题库。

---

## 场景模型(贯穿全计划的核心概念)

诊断与计划的"场景(scene)"按主战场动态生成:
- **现金主战场** → 场景 = format:`cash`/`face3b`/`face4b`/`squeeze`/`coldcall`,显示名取 `FORMATS[f].tag`(现金/面3bet/面4bet/挤压/冷跟)。
- **锦标赛主战场** → 场景 = `mtt` 各 variant 的 `group` 字段(`RFI 开局`/`推弃 · 9人`/`面对全下…`/`单挑 HU…`/`特殊场景`)。

一个 scene 对象统一形如:`{key, name, picks:[{format, variant}, ...]}`,`picks` 是该场景覆盖的所有 spot 坐标。出题时从这些坐标的 table 里抽手。

---

## 文件结构

| 文件 | 职责 | 新建/改 |
|---|---|---|
| `js/coach.js` | 诊断聚合 + 计划编排 + 报告/每日卡片渲染 + onboard/diagnosis/plan 存取 | **新建** |
| `gto-trainer.html` | 主页第 7 卡片 + `#coachScreen`(问卷/诊断/报告/每日卡片各 section) + CSS + `<script src>` | 改 |
| `js/i18n.js` | 新文案(问卷/报告/计划/场景名) | 改 |
| `js/app.js` | `resolve()` 加 `G.diagMode` 隔离分支;`G` 初始化补 `diagMode` | 改 |
| `test/coach.test.js` | `coachScenes`/`coachAggregate`/`coachVerdict`/`coachBuildPlan` 纯逻辑测试 | **新建** |
| `test/load-app.js` | `EXPORTS` 追加 coach 纯函数 | 改 |

加载顺序(html `<script src>`):`… → app.js → coach.js`(coach 依赖 app 的 `buildSmartQueue`/`classifyMiss`/`G`/`PACKS`/`MODES` 等)。

---

## Task 1: 骨架 — coach.js 空壳 + 加载 + 导出 + 测试文件

**Files:**
- Create: `js/coach.js`
- Modify: `gto-trainer.html`(在 `<script src="js/app.js"></script>` 后加一行)
- Create: `test/coach.test.js`
- Modify: `test/load-app.js`(EXPORTS)

- [ ] **Step 1: 建 `js/coach.js`,先放一个最小可测的纯函数 + 导出守卫**

```js
'use strict';
/* coach.js — 翻前诊断 + 20 天训练计划。加载在 app.js 之后,共享全局作用域。
   纯逻辑(coachScenes/coachAggregate/coachVerdict/coachBuildPlan)无 DOM 依赖、可单测;
   渲染函数沿用 app.js 的字符串拼接风格。诚实:全 vs 参考范围,不编 EV/频率。 */

// 计划固定天数(设计已定 20)
const COACH_PLAN_DAYS = 20;

// 占位纯函数,Task 3 替换实现;先让测试管道跑通
function coachPlanDays(){ return COACH_PLAN_DAYS; }

// Node 测试用导出(浏览器忽略)
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { coachPlanDays, COACH_PLAN_DAYS };
}
```

- [ ] **Step 2: html 加载 coach.js**

在 `gto-trainer.html` 里 `app.js` 那一行之后加:

```html
<script src="js/app.js"></script>
<script src="js/coach.js"></script>
```

- [ ] **Step 3: load-app.js 导出 coach 函数**

把 `test/load-app.js` 的 `EXPORTS` 数组末尾(line 47 `'L', 'tr', 'setLang', 'curLang',` 之后)补一行:

```js
  'coachPlanDays', 'coachScenes', 'coachAggregate', 'coachVerdict', 'coachBuildPlan',
```

(后续 Task 会逐个实现这些;此刻只有 `coachPlanDays` 存在,其余为 `undefined`——vm 捕获 `undefined` 不报错。)

- [ ] **Step 4: 建测试文件 `test/coach.test.js`**

```js
'use strict';
const { test } = require('node:test');
const assert = require('node:assert');
const { loadApp } = require('./load-app');
const app = loadApp();

test('coach 骨架已加载', () => {
  assert.strictEqual(typeof app.coachPlanDays, 'function');
  assert.strictEqual(app.coachPlanDays(), 20);
});
```

- [ ] **Step 5: 跑测试,确认通过**

Run: `& "C:\Program Files\nodejs\node.exe" --test "test/coach.test.js"`
Expected: PASS(1 test);并且 `npm test` 全套仍 39+ 通过(没破坏现有)。

- [ ] **Step 6: Commit(待用户说提交)**

```bash
git add js/coach.js test/coach.test.js test/load-app.js gto-trainer.html
git commit -m "feat(coach): 诊断+计划功能骨架(coach.js 加载+测试管道)"
```

---

## Task 2: 场景模型 `coachScenes(onboard)`

**Files:**
- Modify: `js/coach.js`
- Modify: `test/coach.test.js`

- [ ] **Step 1: 写失败测试**(追加到 `test/coach.test.js`)

```js
test('coachScenes: 现金主战场 → 按 format 出 5 个场景', () => {
  const scenes = app.coachScenes({ field: 'cash' });
  const keys = scenes.map(s => s.key);
  assert.ok(keys.includes('cash'));
  assert.ok(keys.includes('face3b'));
  assert.ok(keys.includes('squeeze'));
  // 每个场景都有 picks,且 picks 指向真实存在的 PACKS 坐标
  for (const s of scenes) {
    assert.ok(s.picks.length > 0, `${s.key} 应有 picks`);
    assert.ok(s.name, `${s.key} 应有显示名`);
  }
});

test('coachScenes: 锦标赛主战场 → 按 variant group 出场景', () => {
  const scenes = app.coachScenes({ field: 'mtt' });
  const names = scenes.map(s => s.name);
  // mtt 的 group 之一(出题来源是 PACKS.mtt 实际存在的 variant)
  assert.ok(scenes.length > 0);
  assert.ok(scenes.every(s => s.picks.every(p => p.format === 'mtt')));
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `& "C:\Program Files\nodejs\node.exe" --test "test/coach.test.js"`
Expected: FAIL(`coachScenes is not a function`)

- [ ] **Step 3: 实现 `coachScenes`**(加到 `js/coach.js`,`coachPlanDays` 之后)

```js
// 主战场 → 诊断/计划覆盖的场景列表。每个场景 {key, name, picks:[{format,variant}]}。
// 现金:场景=format(用 FORMATS.tag);锦标赛:场景=mtt variant 的 group。
// picks 只收 PACKS 里真实存在的坐标(空场景跳过)。
function coachScenes(onboard){
  const field = (onboard && onboard.field) || 'cash';
  const out = [];
  if (field === 'mtt') {
    const vs = (PACKS.mtt) ? Object.keys(PACKS.mtt) : [];
    const groups = {};                         // group 名 -> [variant...]
    vs.forEach(v => {
      const meta = VARIANTS.mtt && VARIANTS.mtt[v];
      if (!meta) return;
      const g = meta.group || meta.short || v;
      (groups[g] = groups[g] || []).push(v);
    });
    Object.keys(groups).forEach(g => {
      const picks = groups[g].map(v => ({ format: 'mtt', variant: v }))
        .filter(p => PACKS.mtt[p.variant] && PACKS.mtt[p.variant].length);
      if (picks.length) out.push({ key: 'mtt:' + g, name: g, picks });
    });
  } else {
    const formats = (GAMETYPES.cash && GAMETYPES.cash.formats) || ['cash'];
    formats.forEach(f => {
      if (!PACKS[f]) return;
      const picks = Object.keys(PACKS[f])
        .filter(v => PACKS[f][v] && PACKS[f][v].length)
        .map(v => ({ format: f, variant: v }));
      if (picks.length) out.push({ key: f, name: (FORMATS[f] && FORMATS[f].tag) || f, picks });
    });
  }
  return out;
}
```

- [ ] **Step 4: 把 `coachScenes` 加进导出**(coach.js 底部 `module.exports`)

```js
  module.exports = { coachPlanDays, COACH_PLAN_DAYS, coachScenes };
```

- [ ] **Step 5: 跑测试确认通过**

Run: `& "C:\Program Files\nodejs\node.exe" --test "test/coach.test.js"`
Expected: PASS(3 tests)

- [ ] **Step 6: Commit(待用户说提交)**

```bash
git add js/coach.js test/coach.test.js
git commit -m "feat(coach): coachScenes 按主战场动态生成场景"
```

---

## Task 3: 诊断结果聚合 `coachAggregate(results, scenes)`

诊断答完得到 `results`:每手 `{sceneKey, t, hand, choice, correct}`(`correct`=布尔,是否答对;`t`=spot table)。聚合成 per-scene 正答率 + 漏洞类型计数 + Top 漏洞 + 强项。

**Files:**
- Modify: `js/coach.js`
- Modify: `test/coach.test.js`

- [ ] **Step 1: 写失败测试**

```js
test('coachAggregate: 算每场景正答率 + Top 漏洞 + 强项', () => {
  // 造两个假 spot table(够 classifyMiss 用:需要 R/C/M Set + mode)
  const openT = { mode:'open', name:'t_open', R:new Set(['AA']), C:new Set(), M:new Set() };
  const defT  = { mode:'defense', name:'t_def', R:new Set(), C:new Set(['KK']), M:new Set() };
  const results = [
    { sceneKey:'cash', t:openT, hand:'AA', choice:'raise', correct:true },
    { sceneKey:'cash', t:openT, hand:'AA', choice:'fold',  correct:false }, // 该加却弃 → tight
    { sceneKey:'face3b', t:defT, hand:'KK', choice:'fold', correct:false }, // 该跟却弃 → tight
    { sceneKey:'face3b', t:defT, hand:'KK', choice:'call', correct:true },
  ];
  const scenes = [{key:'cash',name:'现金'},{key:'face3b',name:'面3bet'}];
  const agg = app.coachAggregate(results, scenes);
  assert.strictEqual(agg.perScene.cash.n, 2);
  assert.strictEqual(agg.perScene.cash.correct, 1);
  assert.ok(agg.perScene.cash.acc > 0.49 && agg.perScene.cash.acc < 0.51); // 50%
  // Top 漏洞按正答率升序,且只收样本>=阈值的场景
  assert.ok(Array.isArray(agg.topLeaks));
  assert.ok(agg.topLeaks.every(l => l.leak)); // 每条带漏洞类型
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `& "C:\Program Files\nodejs\node.exe" --test "test/coach.test.js"`
Expected: FAIL(`coachAggregate is not a function`)

- [ ] **Step 3: 实现 `coachAggregate`**

```js
const COACH_MIN_SAMPLE = 4;   // 一个场景样本 < 此数 → 不参与漏洞/强项断言(诚实)

// results: [{sceneKey, t, hand, choice, correct}]。复用 app.js 的 classifyMiss。
function coachAggregate(results, scenes){
  const perScene = {};
  const nameOf = {}; (scenes||[]).forEach(s => nameOf[s.key] = s.name);
  results.forEach(r => {
    const e = perScene[r.sceneKey] || (perScene[r.sceneKey] = { n:0, correct:0, leakCounts:{}, name:nameOf[r.sceneKey]||r.sceneKey });
    e.n++;
    if (r.correct) { e.correct++; }
    else {
      const leak = classifyMiss({ t:r.t, hand:r.hand, choice:r.choice, variant:r.variant });
      e.leakCounts[leak] = (e.leakCounts[leak] || 0) + 1;
    }
  });
  Object.values(perScene).forEach(e => { e.acc = e.n ? e.correct / e.n : 0; });
  // Top 漏洞:样本足够的场景按正答率升序,取最弱的前 3;主漏洞类型=该场景错得最多的桶
  const eligible = Object.keys(perScene).filter(k => perScene[k].n >= COACH_MIN_SAMPLE);
  const topLeaks = eligible
    .filter(k => perScene[k].acc < 0.7)
    .sort((a,b) => perScene[a].acc - perScene[b].acc)
    .slice(0, 3)
    .map(k => {
      const lc = perScene[k].leakCounts;
      const leak = Object.keys(lc).sort((a,b) => lc[b]-lc[a])[0] || 'mix';
      return { sceneKey:k, name:perScene[k].name, acc:perScene[k].acc, leak };
    });
  // 强项:样本足够且正答率最高的前 2(>=0.7)
  const strengths = eligible
    .filter(k => perScene[k].acc >= 0.7)
    .sort((a,b) => perScene[b].acc - perScene[a].acc)
    .slice(0, 2)
    .map(k => ({ sceneKey:k, name:perScene[k].name, acc:perScene[k].acc }));
  return { perScene, topLeaks, strengths };
}
```

- [ ] **Step 4: 导出**

```js
  module.exports = { coachPlanDays, COACH_PLAN_DAYS, coachScenes, coachAggregate, COACH_MIN_SAMPLE };
```

- [ ] **Step 5: 跑测试确认通过**

Run: `& "C:\Program Files\nodejs\node.exe" --test "test/coach.test.js"`
Expected: PASS

- [ ] **Step 6: Commit(待用户说提交)**

```bash
git add js/coach.js test/coach.test.js
git commit -m "feat(coach): coachAggregate 聚合诊断结果(正答率/Top漏洞/强项)"
```

---

## Task 4: 总评措辞 `coachVerdict(agg, variant)`(随题数自适应)

**Files:**
- Modify: `js/coach.js`
- Modify: `test/coach.test.js`

- [ ] **Step 1: 写失败测试**

```js
test('coachVerdict: 简化版只给大方向、不点名场景', () => {
  const agg = { perScene:{}, topLeaks:[{sceneKey:'face3b',name:'面3bet',acc:0.4,leak:'tight'}], strengths:[] };
  const v = app.coachVerdict(agg, 'simple');
  assert.ok(v.headline);           // 有总评
  assert.strictEqual(v.detailed, false); // 简化版标记为非细分
});

test('coachVerdict: 详细版可点名漏洞', () => {
  const agg = { perScene:{}, topLeaks:[{sceneKey:'face3b',name:'面3bet',acc:0.4,leak:'tight'}], strengths:[] };
  const v = app.coachVerdict(agg, 'full');
  assert.strictEqual(v.detailed, true);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `& "C:\Program Files\nodejs\node.exe" --test "test/coach.test.js"`
Expected: FAIL

- [ ] **Step 3: 实现 `coachVerdict`**(`tone`/`headline` 是 i18n key,Task 10 填文案;这里只定逻辑与 key)

```js
// 由漏洞类型分布合成"偏松/偏紧/较均衡"倾向。返回 {tendKey, headline(i18n key), detailed}。
// detailed: 详细版(full)才允许 UI 点名具体场景;简化版只给大方向。
function coachVerdict(agg, variant){
  let lo=0, ti=0;
  Object.values(agg.perScene || {}).forEach(e => {
    Object.keys(e.leakCounts||{}).forEach(k => {
      if (k==='loose') lo += e.leakCounts[k];
      else if (k==='tight') ti += e.leakCounts[k];
    });
  });
  let tendKey;
  if (lo + ti < 3) tendKey = 'coachTendBalanced';
  else if (lo >= ti*1.6) tendKey = 'coachTendLoose';
  else if (ti >= lo*1.6) tendKey = 'coachTendTight';
  else tendKey = 'coachTendBalanced';
  return { tendKey, headline: tendKey, detailed: variant === 'full' };
}
```

- [ ] **Step 4: 导出** — 把 `coachVerdict` 加进 `module.exports`。

- [ ] **Step 5: 跑测试确认通过**

Run: `& "C:\Program Files\nodejs\node.exe" --test "test/coach.test.js"`
Expected: PASS

- [ ] **Step 6: Commit(待用户说提交)**

```bash
git add js/coach.js test/coach.test.js
git commit -m "feat(coach): coachVerdict 总评措辞随题数自适应"
```

---

## Task 5: 计划编排 `coachBuildPlan(agg, onboard, scenes)`

固定 20 天:前 13 天按 Top 漏洞优先级逐个攻克(按漏洞数比例分天),后 7 天全场景混合。退化:无漏洞→按正答率升序铺场景;目标=系统过一遍→均匀覆盖。

**Files:**
- Modify: `js/coach.js`
- Modify: `test/coach.test.js`

- [ ] **Step 1: 写失败测试**

```js
test('coachBuildPlan: 固定 20 天,前段攻漏洞、后段混合', () => {
  const agg = {
    perScene:{ face3b:{acc:0.4,name:'面3bet'}, cash:{acc:0.55,name:'现金'} },
    topLeaks:[
      {sceneKey:'face3b',name:'面3bet',acc:0.4,leak:'tight'},
      {sceneKey:'cash',name:'现金',acc:0.55,leak:'tight'},
    ],
    strengths:[],
  };
  const scenes = [{key:'face3b',name:'面3bet'},{key:'cash',name:'现金'}];
  const plan = app.coachBuildPlan(agg, { minutes:10, goal:'leak' }, scenes);
  assert.strictEqual(plan.days.length, 20);
  // 前段是漏洞场景,最后 7 天是 mixed
  assert.strictEqual(plan.days[0].sceneKey, 'face3b');   // 最严重的先上
  assert.ok(plan.days.slice(13).every(d => d.mixed === true));
  // 每天有训练手数(由 minutes 定)
  assert.strictEqual(plan.days[0].nMain, 20);            // 10min → 20 手
  assert.ok(plan.streak === 0 && plan.curDay === 0);
});

test('coachBuildPlan: 无明显漏洞 → 退化按正答率升序铺场景', () => {
  const agg = {
    perScene:{ a:{acc:0.8,name:'A'}, b:{acc:0.75,name:'B'} },
    topLeaks:[], strengths:[],
  };
  const scenes = [{key:'a',name:'A'},{key:'b',name:'B'}];
  const plan = app.coachBuildPlan(agg, { minutes:5, goal:'leak' }, scenes);
  assert.strictEqual(plan.days.length, 20);
  assert.strictEqual(plan.days[0].nMain, 10);            // 5min → 10 手
  assert.strictEqual(plan.days[0].sceneKey, 'b');        // 较弱的(0.75)先
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `& "C:\Program Files\nodejs\node.exe" --test "test/coach.test.js"`
Expected: FAIL

- [ ] **Step 3: 实现 `coachBuildPlan`**

```js
const COACH_FOCUS_DAYS = 13;                       // 前段攻克天数(后 7 天混合)
const COACH_MIN_PER_SCENE = 5;                     // 复习手数上限(每日)
function coachHandsForMinutes(m){ return m>=20?40 : m>=10?20 : 10; }

// agg: coachAggregate 输出;onboard: 问卷;scenes: coachScenes 输出。
// 返回 {createdTs:0, days:[{idx, sceneKey, name, nMain, nReview, mixed, done:false}], streak:0, curDay:0, lastDoneDate:null}
function coachBuildPlan(agg, onboard, scenes){
  const nMain = coachHandsForMinutes((onboard && onboard.minutes) || 10);
  const sceneName = {}; (scenes||[]).forEach(s => sceneName[s.key] = s.name);

  // 1. 前段场景队列
  let focusQueue;
  const goal = onboard && onboard.goal;
  if (goal === 'system') {
    // 系统过一遍:均匀覆盖所有场景
    focusQueue = (scenes||[]).map(s => s.key);
  } else if (agg.topLeaks && agg.topLeaks.length) {
    // 漏洞优先:按严重度(正答率升序)
    focusQueue = agg.topLeaks.map(l => l.sceneKey);
  } else {
    // 退化:无明显漏洞,按正答率升序铺所有场景
    focusQueue = Object.keys(agg.perScene||{})
      .sort((a,b) => (agg.perScene[a].acc||0) - (agg.perScene[b].acc||0));
    if (!focusQueue.length) focusQueue = (scenes||[]).map(s => s.key);
  }
  if (!focusQueue.length) focusQueue = ['mixed'];

  // 2. 前 13 天按队列循环铺(漏洞越靠前,占的天越多——靠循环顺序自然加权:第1个出现在 day0、day_q、day_2q…)
  const days = [];
  for (let i = 0; i < COACH_FOCUS_DAYS; i++) {
    const key = focusQueue[i % focusQueue.length];
    days.push({ idx:i, sceneKey:key, name:sceneName[key]||key, nMain, nReview:COACH_MIN_PER_SCENE, mixed:false, done:false });
  }
  // 3. 后 7 天混合巩固
  for (let i = COACH_FOCUS_DAYS; i < COACH_PLAN_DAYS; i++) {
    days.push({ idx:i, sceneKey:'mixed', name:'mixed', nMain, nReview:COACH_MIN_PER_SCENE, mixed:true, done:false });
  }
  return { createdTs:0, days, streak:0, curDay:0, lastDoneDate:null };
}
```

> 注:`days[0]` 是 `focusQueue[0]`=最严重漏洞,满足测试。"按比例分天"用循环取模实现(最严重的排在最前、且每轮第一个,自然占更多天),避免复杂的比例切分逻辑(YAGNI)。

- [ ] **Step 4: 导出** — 把 `coachBuildPlan`、`coachHandsForMinutes` 加进 `module.exports`。

- [ ] **Step 5: 跑测试确认通过**

Run: `& "C:\Program Files\nodejs\node.exe" --test "test/coach.test.js"`
Expected: PASS

- [ ] **Step 6: Commit(待用户说提交)**

```bash
git add js/coach.js test/coach.test.js
git commit -m "feat(coach): coachBuildPlan 编排固定 20 天计划(漏洞优先+退化+目标变体)"
```

---

## Task 6: 数据存取 — onboard / diagnosis / plan 持久化

复用 `app.js` 的 `STORE`(同一 `STORE_KEY='gtoTrainer_v1'`)+ `persist()`。

**Files:**
- Modify: `js/coach.js`

- [ ] **Step 1: 实现存取函数**(加到 coach.js;无新测试——纯 localStorage 包装,靠 Task 11 手动验证)

```js
// 全部挂在现有 STORE 对象下,复用 app.js 的 persist()
function coachLoadOnboard(){ return (typeof STORE!=='undefined' && STORE.coachOnboard) || null; }
function coachSaveOnboard(o){ STORE.coachOnboard = o; persist(); }
function coachLoadDiagnosis(){ return (typeof STORE!=='undefined' && STORE.coachDiagnosis) || null; }
function coachSaveDiagnosis(d){ STORE.coachDiagnosis = d; persist(); }
function coachLoadPlan(){ return (typeof STORE!=='undefined' && STORE.coachPlan) || null; }
function coachSavePlan(p){ STORE.coachPlan = p; persist(); }
function coachResetAll(){ delete STORE.coachOnboard; delete STORE.coachDiagnosis; delete STORE.coachPlan; persist(); }
```

- [ ] **Step 2: 跑全套测试确认没破坏**

Run: `npm test`(= `node --test "test/*.test.js"`,用全路径 node)
Expected: 全通过(coach 存取不影响纯逻辑测试)

- [ ] **Step 3: Commit(待用户说提交)**

```bash
git add js/coach.js
git commit -m "feat(coach): onboard/diagnosis/plan 持久化(复用 STORE)"
```

---

## Task 7: 诊断模式隔离 — app.js `resolve()` 加 `G.diagMode` 分支

诊断走现有游戏循环出题/答题,但**不计 HP、不进生涯统计、不进常规错题堆**,而是收集到 `G.diagResults`。

**Files:**
- Modify: `js/app.js`(`resolve` 函数 line 522 起;`G` 初始化)
- Modify: `js/coach.js`(诊断 runner)

- [ ] **Step 1: 先读 `resolve()` 全文**,定位它在哪里:① 扣 HP ② 写 `statsBySpot` ③ 调 `addMistake`。

Run: `& "C:\Program Files\nodejs\node.exe" -e "const fs=require('fs');const s=fs.readFileSync('js/app.js','utf8').split('\n').slice(521,600).join('\n');console.log(s)"`

- [ ] **Step 2: 在 `resolve()` 最开头插入诊断分支**

在 `function resolve(choice,btn,timedOut){` 之后、原逻辑之前,加:

```js
 if(G.diagMode){
  // 诊断模式:只记录对错,不扣 HP / 不写统计 / 不进错题堆
  const t=G.table, hand=G.hand;
  const isR=t.R.has(hand),isC=t.C.has(hand),isM=t.M.has(hand);
  const correctSet=MODES[t.mode].correct(isR,isC,isM);
  const ok=correctSet.includes(choice);
  G.diagResults.push({ sceneKey:G.diagSceneKey, t, hand, choice, correct:ok, variant:G.variant });
  G.hands++; if(ok)G.correct++;
  coachDiagAdvance();   // 推进诊断:下一手或结束(在 coach.js)
  return;
 }
```

- [ ] **Step 3: `G` 初始化补字段**

在 `newGame()`(line 217)和诊断启动处保证 `G.diagMode` 默认 false。最稳:在 app.js 定义 `G` 的地方(搜 `const G=` 或 `let G=`)确认有 `diagMode:false`;若 `G` 是字面量对象就补一行 `diagMode:false,`。Run 定位:

`& "C:\Program Files\nodejs\node.exe" -e "const fs=require('fs');const m=fs.readFileSync('js/app.js','utf8').match(/(const|let|var)\s+G\s*=/);console.log(m?m.index:'not found')"`

(`G` 通常是个大字面量;在其中加 `diagMode:false, diagResults:[], diagSceneKey:null,`。)

- [ ] **Step 4: coach.js 写诊断 runner**(出题用现有引擎,但跨场景)

```js
// 诊断出题:从 scenes 的 picks 里,按场景均衡抽够 total 手。复用 buildSmartQueue 的题库构造思路,
// 但跨 format/variant。每手 {sceneKey, format, variant, t, hand}。
function coachBuildDiagQueue(scenes, total){
  const perScene = Math.max(1, Math.round(total / scenes.length));
  const queue = [];
  scenes.forEach(s => {
    const cands = [];
    s.picks.forEach(p => {
      const pack = PACKS[p.format] && PACKS[p.format][p.variant];
      if (!pack) return;
      pack.forEach(t => {
        // 取范围内手牌(R∪C∪M)+ 少量难弃牌
        const inRange = [...(t.union||new Set())];
        inRange.forEach(h => cands.push({ sceneKey:s.key, format:p.format, variant:p.variant, t, hand:h }));
        (typeof adjacentFolds==='function' ? adjacentFolds(t) : []).slice(0,3)
          .forEach(h => cands.push({ sceneKey:s.key, format:p.format, variant:p.variant, t, hand:h }));
      });
    });
    shuffle(cands);
    queue.push(...cands.slice(0, perScene));
  });
  return shuffle(queue);
}

// 启动诊断:variant='simple'(18)|'full'(45)。设置 G.diagMode,发第一手。
let _coachDiagQueue = null, _coachDiagPos = 0, _coachDiagOnReport = null;
function coachStartDiagnosis(onboard, variant, onReport){
  const scenes = coachScenes(onboard);
  const total = variant === 'full' ? 45 : 18;
  _coachDiagQueue = coachBuildDiagQueue(scenes, total);
  _coachDiagPos = 0;
  _coachDiagOnReport = onReport;
  G.diagMode = true; G.diagResults = []; G.over=false; G.busy=false;
  G.hands=0; G.correct=0; G.diagVariant = variant; G.diagScenes = scenes;
  coachDiagDeal();
}
// 发当前手:把 G 指到对应 spot,然后复用 nextHand 的呈现?——诊断自管发牌,直接设 G 字段后渲染。
function coachDiagDeal(){
  if (_coachDiagPos >= _coachDiagQueue.length){ coachFinishDiagnosis(); return; }
  const item = _coachDiagQueue[_coachDiagPos];
  G.format=item.format; G.variant=item.variant; G.table=item.t; G.hand=item.hand;
  G.diagSceneKey=item.sceneKey;
  // 计算 correct_set 供 UI 用现有判定(和 nextHand 一致)
  const t=item.t, h=item.hand;
  G.correct_set = MODES[t.mode].correct(t.R.has(h),t.C.has(h),t.M.has(h));
  coachRenderDiagHand();    // Task 8 的 UI 渲染
}
function coachDiagAdvance(){ _coachDiagPos++; coachDiagDeal(); }
function coachFinishDiagnosis(){
  G.diagMode=false;
  const agg = coachAggregate(G.diagResults, G.diagScenes);
  const verdict = coachVerdict(agg, G.diagVariant);
  const diagnosis = { variant:G.diagVariant, agg, verdict, ts:0 };
  coachSaveDiagnosis(diagnosis);
  if (_coachDiagOnReport) _coachDiagOnReport(diagnosis);
}
```

> 注:诊断自管发牌(不走 `nextHand`),避免 `nextHand` 里的 HP/queue 逻辑干扰。`coachRenderDiagHand`/`coachRenderDiagHand` 的 UI 在 Task 8。

- [ ] **Step 5: 跑全套测试确认 app.js 改动没破坏现有**

Run: `npm test`
Expected: 39+ 全通过(诊断分支只在 `G.diagMode` 时触发,正常训练路径不变;金快照不受影响)

- [ ] **Step 6: Commit(待用户说提交)**

```bash
git add js/app.js js/coach.js
git commit -m "feat(coach): 诊断模式隔离(resolve 分支 + 跨场景诊断 runner)"
```

---

## Task 8: UI — 问卷 / 诊断答题 / 报告 / 每日卡片

复用现有 `.chart-screen` 覆盖层模式 + `renderLeak` 风格的字符串拼接。HTML 加一个 `#coachScreen`,内含 4 个 section(问卷/诊断/报告/每日),靠 class 切换显示。

**Files:**
- Modify: `gto-trainer.html`(加 `#coachScreen` + CSS)
- Modify: `js/coach.js`(渲染函数)

- [ ] **Step 1: html 加 `#coachScreen` 骨架**(放在 `#nashScreen` 同级,`.chart-screen` 覆盖层)

```html
<div class="screen chart-screen hide" id="coachScreen">
  <button class="back-btn" id="coachBack">← 返回</button>
  <h2 class="chart-title" id="coachTitle">训练计划</h2>
  <div id="coachOnboard"></div>   <!-- 问卷 -->
  <div id="coachDiag" class="hide"></div>    <!-- 诊断答题 -->
  <div id="coachReport" class="hide"></div>  <!-- 报告 -->
  <div id="coachDay" class="hide"></div>     <!-- 每日卡片 -->
</div>
```

- [ ] **Step 2: coach.js 写四个渲染函数 + 入口路由**

实现(沿用 mockup.html 的版式 + 现有 `renderLeak` 的拼接风格;完整 HTML 字符串):
- `coachOpen()` — 入口:判定状态(无 onboard→问卷;有 plan→当天卡片;计划完成→完成页),显示 `#coachScreen`,隐藏其他。
- `coachRenderOnboard()` — 4 题问卷(单选 chip),选完存 `coachSaveOnboard`,进版本选择(简化/详细),点击调 `coachStartDiagnosis(onboard, variant, coachRenderReport)`。
- `coachRenderDiagHand()` — 诊断答题:复用现有发牌卡片 + 动作按钮(读 `MODES[G.table.mode].actions`),按钮 onclick 调 `choose(key)`(已有,会走 resolve 的 diagMode 分支);顶部进度 `_coachDiagPos+1 / _coachDiagQueue.length`。
- `coachRenderReport(diagnosis)` — 报告(mockup 屏1):总评(`tr(diagnosis.verdict.headline)`)+ 场景条形(`perScene` 正答率,颜色按 acc 分档)+ Top3 漏洞(`topLeaks`,`tr('leakType_'+leak)`)+ 强项 + 计划概览 + 「开始 Day 1」按钮。简化版(`!verdict.detailed`)隐藏场景级断言、加"快速估计"标注。底部注脚 `基于 N 手 · vs 参考范围`。「开始 Day 1」onclick:`if(!isPro()){showPaywall(tr('pwWhyPlan'));return;} coachStartPlan(diagnosis);`
- `coachRenderDay()` — 每日卡片(mockup 屏2):读 `coachLoadPlan()`,显示当天 `days[curDay]`(主题名 `tr` / 进度 / streak / 本周列表),「开始今天」onclick → `coachStartDayTraining(day)`。

每个函数的 HTML 拼接参照 mockup.html 的 class 命名(`.card`/`.lbl`/`.bar-row`/`.leak`/`.seg`/`.prog`),CSS 在 Step 4 一并加。

> 完整渲染代码较长,实现时直接把 mockup.html 的 DOM 结构转成模板字符串,数据用 `diagnosis.agg.perScene` 等填充。颜色分档:`acc>=.7`绿 / `.55-.69`黄 / `.4-.54`橙 / `<.4`红(同 mockup)。

- [ ] **Step 3: 计划启动 + 打卡逻辑(coach.js)**

```js
function coachStartPlan(diagnosis){
  const onboard = coachLoadOnboard();
  const plan = coachBuildPlan(diagnosis.agg, onboard, coachScenes(onboard));
  plan.createdTs = 0;
  coachSavePlan(plan);
  coachRenderDay();
}
// 今日训练:用计划当天的场景出题,练完标记完成 + streak
function coachStartDayTraining(day){
  const plan = coachLoadPlan();
  // 复用现有训练:设 format/variant 到该场景的一个 pick,走 newGame——但要在练满 nMain 手后回调打卡。
  // 简化:用诊断同款自管发牌(coachBuildDiagQueue 单场景),练完调 coachMarkDayDone。
  // (实现细节:可复用 buildSmartQueue,在该场景的 spot 上;练满 day.nMain+day.nReview 手触发 coachMarkDayDone。)
  // 详见实现注释。
}
function coachMarkDayDone(accuracy){
  const plan = coachLoadPlan(); if(!plan) return;
  const d = plan.days[plan.curDay]; if(!d) return;
  d.done = true; d.accuracy = accuracy;
  // streak:连续日才 +1(lastDoneDate 是昨天则续,否则重置为 1)。日期用 app.js 现有的日期工具或 toDateStr。
  plan.streak = (plan.streak||0) + 1;
  plan.curDay = Math.min(plan.curDay + 1, plan.days.length);
  coachSavePlan(plan);
  coachRenderDay();
}
```

> 注:`coachStartDayTraining` 的"练满 N 手回调"实现时,在游戏循环里加一个轻量计数(类似诊断的 `_coachDiagPos`),或复用 `SESSION_HANDS` 机制把会话长度临时设为 `nMain+nReview`。**不锁门**:每日卡片只读 `curDay`,用户可手动跳。日期比较用本地 `new Date().toDateString()`(浏览器环境可用;测试不覆盖 UI)。

- [ ] **Step 4: CSS**

把 mockup.html `<style>` 里 `.bar-row/.bar-track/.bar-fill/.leak/.leak-n/.tag/.chip/.seg/.seg-ic/.prog/.streak/.theme/.verdict` 这些类,适配进 `gto-trainer.html` 的 CSS 区(用现有 `--gold/--green/--red/--panel/--line/--muted` 变量,删掉 mockup 里重复定义的 `:root`)。

- [ ] **Step 5: 手动浏览器验证**(无自动测试覆盖 DOM)

打开 `gto-trainer.html` → 主页(Task 9 后)→ 训练计划 → 走完问卷 → 简化版诊断 18 手 → 报告 → 开始 Day 1 → 每日卡片。确认:诊断不扣 HP、报告数字合理、计划 20 天。

- [ ] **Step 6: Commit(待用户说提交)**

```bash
git add gto-trainer.html js/coach.js
git commit -m "feat(coach): 问卷/诊断/报告/每日卡片 UI"
```

---

## Task 9: 主页第 7 张卡片 + 入口路由 + Pro 门控

**Files:**
- Modify: `gto-trainer.html`(homeScreen 加第 7 卡)
- Modify: `js/coach.js`(在主页菜单 IIFE 之外自挂 onclick,或暴露 `coachOpen` 供 app.js IIFE 调)
- Modify: `js/app.js`(主页菜单 IIFE 加 `bind('homePlan', ...)`)

- [ ] **Step 1: html 加第 7 张卡**(在 `homeStats` 之后)

```html
<button class="home-card" id="homePlan"><span class="hc-ic">🗓</span><span class="hc-t">训练计划</span><span class="hc-s">诊断 + 20 天</span></button>
```

(7 张卡:grid 2 列会排成 3 行 + 第 7 张单独一行;CSS `.home-grid` 已是 `1fr 1fr`,第 7 张靠左。若要居中,给 `#homePlan` 加 `grid-column:1/-1;max-width:50%;margin:0 auto` —— 视觉实现时定。)

- [ ] **Step 2: app.js 主页菜单 IIFE 加绑定**

在主页菜单 IIFE(`js/app.js` 末尾,`bind('homeStats',...)` 附近)加:

```js
 bind('homePlan', ()=>{ goStart(); if(typeof coachOpen==='function') coachOpen(); });
```

(`coachOpen` 内部自己显示 `#coachScreen`、隐藏 `startScreen`。)

- [ ] **Step 3: coach.js 接 back 按钮 + i18n 标签**

```js
try{ document.getElementById('coachBack').onclick=()=>{ try{SFX.click();}catch(e){} document.getElementById('coachScreen').classList.add('hide'); }; }catch(e){}
```

- [ ] **Step 4: Pro 门控验证** — 报告页「开始 Day 1」按钮已在 Task 8 接 `isPro()` 门控(网页恒解锁、native 未购弹 `showPaywall`)。确认 `pwWhyPlan` 文案在 Task 10 补。

- [ ] **Step 5: 手动验证主页 7 卡 + 入口**

打开 app → 主页应有 7 张卡 → 点「训练计划」进 coachScreen。

- [ ] **Step 6: Commit(待用户说提交)**

```bash
git add gto-trainer.html js/app.js js/coach.js
git commit -m "feat(coach): 主页第7卡片训练计划入口 + Pro 门控"
```

---

## Task 10: i18n 全文案(中英双语)

**Files:**
- Modify: `js/i18n.js`

- [ ] **Step 1: 加 `_tpl` 文案**(在 nash 文案附近)

至少覆盖(中文, 英文):
```js
_tpl('coachTitle','训练计划','Training Plan');
_tpl('coachTendLoose','偏松','Loose-leaning');
_tpl('coachTendTight','偏紧','Tight-leaning');
_tpl('coachTendBalanced','较均衡','Balanced');
_tpl('coachQ1','主战场','Main game');
_tpl('coachQ2','自评水平','Self-rated level');
_tpl('coachQ3','每天能练多久','Daily practice time');
_tpl('coachQ4','主要目标','Main goal');
_tpl('coachSimple','简化版 · 18 手','Quick · 18 hands');
_tpl('coachFull','详细版 · 45 手','Detailed · 45 hands');
_tpl('coachStartDay1','开始 Day 1','Start Day 1');
_tpl('coachQuickEst','快速估计 · 仅供参考','Quick estimate · directional only');
_tpl('coachVsRef','基于 {n} 手诊断 · 全部 vs 参考范围,非 solver 精确解','Based on {n} hands · all vs reference ranges, not solver-exact');
_tpl('pwWhyPlan','20 天个性化训练计划是 Pro 功能','The 20-day personalized plan is a Pro feature');
_tpl('coachStartToday','开始今天','Start today');
_tpl('coachDayN','Day {d} / 20','Day {d} / 20');
_tpl('coachStreak','🔥 {n} 天','🔥 {n} days');
// 漏洞类型名(报告 Top 漏洞用,复用 LEAK_TYPES 但给 i18n key)
_tpl('leakType_loose','太松','Too loose');
_tpl('leakType_tight','太紧','Too tight');
_tpl('leakType_passive','被动','Passive');
_tpl('leakType_aggro','过激','Over-aggressive');
_tpl('leakType_mix','边缘混合','Marginal/mixed');
_tpl('leakType_icm','ICM 保命','ICM survival');
```
(其余问卷选项文案、场景名同样补;场景名优先复用现有 `FORMATS.tag` 的 `L()` 翻译,mtt group 名按需加。)

- [ ] **Step 2: 跑测试**(i18n 改动不影响金快照——数据层仍中文)

Run: `npm test`
Expected: 全通过

- [ ] **Step 3: 手动切中英文验证 coachScreen 无残留**

打开 app → 训练计划 → 切「中/EN」→ 问卷/报告/卡片文案都跟着切。

- [ ] **Step 4: Commit(待用户说提交)**

```bash
git add js/i18n.js
git commit -m "feat(coach): 诊断+计划双语文案"
```

---

## Task 11: 端到端验证 + 回归 + 部署

**Files:** 无代码改动(除非发现 bug)

- [ ] **Step 1: 全套回归**

Run: `npm test`
Expected: 现有 39 + coach 新增测试全通过;金快照无变化(coach 不动 MODES/范围)。

- [ ] **Step 2: 模拟器/浏览器端到端走查**(参照 CLAUDE.md 的构建+装机流程)

走完整流程两遍(中文 + 英文):主页第7卡 → 问卷4题 → 简化版诊断(确认不扣 HP、不进生涯统计)→ 报告(数字合理、措辞带"快速估计")→ 开始 Day1(网页直接进、native 未购弹墙)→ 每日卡片打卡 → streak +1。再跑一遍详细版 45 手。确认「重新诊断」能覆盖。

- [ ] **Step 3: 诚实自查**

人工核对:报告/卡片所有数字都是"正答率",无任何 EV/精确频率字样;所有场景标注 vs 参考范围;简化版不点名具体场景断言。

- [ ] **Step 4: 部署(待用户说提交/部署)**

```bash
bash deploy.sh "新增翻前诊断+20天训练计划:问卷→诊断(简18/详45)→报告(免费)→Pro每日计划"
```

---

## Self-Review(写计划后自查,已修正)

- **Spec 覆盖**:① 流程→Task 7/8/9 ② 问卷→Task 8 + onboard 存储 Task 6 ③ 诊断(复用 buildSmartQueue/classifyMiss + 隔离)→Task 3/7 ④ 报告(措辞自适应)→Task 4/8 ⑤ 20天计划→Task 5/8 ⑥ 数据→Task 6 ⑦ 主页第7卡 + 旧统计保留→Task 9(旧的不动)⑧ 诚实→贯穿 + Task 11 自查 ⑨ 免费/Pro→Task 8/9。全部有对应 task。
- **类型一致**:`coachAggregate` 产出 `{perScene, topLeaks, strengths}` → `coachVerdict`/`coachBuildPlan` 消费的字段名一致(`perScene[k].acc`/`leakCounts`、`topLeaks[].sceneKey`)。诊断 result 形状 `{sceneKey,t,hand,choice,correct,variant}` 在 Task 3 测试、Task 7 生产端一致。
- **占位扫描**:Task 8 的渲染函数与 Task 8/Step3 的 `coachStartDayTraining` 标了"实现细节见注释"——这是 UI/游戏循环集成,非纯逻辑,给了明确的复用对象(mockup DOM 结构 + buildSmartQueue + 会话计数);可接受的实现自由度,不是 TBD。
- **诚实**:报告百分比语义=正答率(非 EV);`coachVerdict` 简化版 `detailed:false` 强制 UI 不点名;`coachVsRef` 注脚。

🐿️ 松鼠出品 · 诚实 > 花哨
