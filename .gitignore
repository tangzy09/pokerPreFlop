# dependencies (none currently, but in case any are ever added)
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/

# scratch
/tmp/
*.log

# local deploy script (contains EC2 host + key path — keep out of the public repo)
/deploy.sh
*.pem

# Python
__pycache__/
*.pyc

# Capacitor staging (generated by tools/build-www.js); android/ keeps its own .gitignore for build artifacts
/www/
